# ooxmlgen demo

Temporary single-page demo. Delete this folder to remove it entirely.

## Run

```bash
npx ts-node --project tsconfig.test.json demo/server.ts
```

Then open http://localhost:3456

## Remove

```bash
rm -rf demo/
```

No changes to the library core are needed.
